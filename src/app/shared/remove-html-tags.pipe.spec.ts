import { RemoveHtmlTagsPipe } from './remove-html-tags.pipe';

describe('RemoveHtmlTagsPipe', () => {
    it('create an instance', () => {
        const pipe = new RemoveHtmlTagsPipe();
        expect(pipe).toBeTruthy();
    });
});
